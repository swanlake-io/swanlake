# SwanLake Server

This crate contains the main binary for the SwanLake server. It initializes and runs the Arrow Flight SQL service using the logic from the `swanlake-core` crate.

## Running the Server

To build and run the server:

```bash
cargo run --bin swanlake
```
