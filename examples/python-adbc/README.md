# Python ADBC Example for SwanLake

This example demonstrates how to connect to SwanLake Flight SQL server using Python and the ADBC (Arrow Database Connectivity) driver.

## Prerequisites

SwanLake server running (default: `localhost:4214`)

## Installation

```bash
# Install dependencies
uv sync

# Run the example
uv run main.py
```
